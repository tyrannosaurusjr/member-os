This directory holds historical code that is no longer part of the active runtime.

Current status:

- `backend-fastapi-legacy/`: earlier FastAPI scaffold kept only for reference during the Django transition

The live application is the Django project at the repository root.
